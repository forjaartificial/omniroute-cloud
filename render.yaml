services:
  - type: web
    name: omniroute-cloud
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OMNIROUTE_SERVER_HOST
        value: 0.0.0.0
      - key: NODE_ENV
        value: production
